{
  "name": "video-processor",
  "version": "1.0.0",
  "scripts": {
    "preinstall": "mkdir -p node_modules/.bin && chmod -R 755 node_modules/.bin",
    "build": "npx vite build",
    "dev": "npx vite",
    "vercel-build": "chmod -R 755 node_modules/.bin && npx vite build"
  },
  "dependencies": {
    "@ffmpeg/ffmpeg": "^0.11.6",
    "react": "^18.0.0",
    "react-dom": "^18.0.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^3.1.0",
    "vite": "^4.5.14"
  },
  "engines": {
    "node": ">=18.18.0"
  }
}
