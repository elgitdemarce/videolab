{
  "name": "video-processor",
  "version": "1.0.0",
  "scripts": {
    "build": "chmod -R 755 node_modules/.bin && npx vite build",
    "dev": "chmod -R 755 node_modules/.bin && npx vite",
    "vercel-build": "chmod -R 755 node_modules/.bin && npx vite build",
    "postinstall": "chmod -R 755 node_modules/.bin"
  },
  "dependencies": {
    "@ffmpeg/ffmpeg": "^0.11.6",
    "react": "^18.0.0",
    "react-dom": "^18.0.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^3.1.0",
    "vite": "^4.5.14"
  }
}
